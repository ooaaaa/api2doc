# 部分更新用户

- **请求方式**: `PATCH`
- **请求路径**: `/api/restful/users/{id}`

## 接口描述

PATCH 请求示例 - 部分更新用户资源（只需提供要修改的字段），适用于只需要更新部分信息的场景

## 路径参数

| 字段 | 字段描述 | 示例 | 类型 | 是否必填 |
| --- | --- | --- | --- | --- |
| id | 要更新的用户ID | 1 | integer | 是 |

## 请求体

| 字段 | 字段描述 | 示例 | 类型 | 是否必填 |
| --- | --- | --- | --- | --- |
| name | 用户姓名（可选） | - | string | 否 |
| email | 用户邮箱（可选） | - | string | 否 |
| age | 用户年龄（可选） | - | integer | 否 |
| status | 用户状态（可选） | - | string | 否 |

### 示例

```json
{
  "name": "张三三",
  "email": "zhangsan_updated@example.com",
  "age": 30,
  "status": "inactive"
}
```

## 响应参数

| 字段 | 字段描述 | 示例 | 类型 | 是否必填 |
| --- | --- | --- | --- | --- |
| success | - | - | boolean | 否 |
| message | - | - | string | 否 |
| data | - | - | object | 否 |
| id | - | - | integer | 否 |
| name | - | - | string | 否 |
| email | - | - | string | 否 |
| age | - | - | integer | 否 |
| status | - | - | string | 否 |
| updatedFields | 本次更新的字段列表 | - | array<string> | 否 |

### 示例

```json
{
  "success": true,
  "message": "用户部分更新成功",
  "data": {
    "id": 1,
    "name": "张三三",
    "email": "zhangsan@example.com",
    "age": 30,
    "status": "inactive"
  },
  "updatedFields": [
    "string"
  ]
}
```

## cURL 示例

```bash
curl -X PATCH 'http://localhost:3010/api/restful/users/{id}' \
  -H 'Content-Type: application/json' \
  -d '{
  "name": "张三三",
  "email": "zhangsan_updated@example.com",
  "age": 30,
  "status": "inactive"
}'
```

